name: perf-materialize-smoke

on:
  push:
    branches:
      - perf/kv-spec-speedups
    paths:
      - .github/workflows/perf-materialize-smoke.yml

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo perf-materialize-runner-ok
